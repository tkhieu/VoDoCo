# Báo cáo kết quả fine-tune correction hậu ASR trên VietMed

- **Dự án:** VoDoCo
- **Ngày báo cáo:** 06/09/2026
- **Thí nghiệm:** `experiments/002-vietmed-correction-training/`
- **Protocol chính:** [RESEARCH_PLAN.md](RESEARCH_PLAN.md)
- **Trạng thái kỹ thuật:** Đã hoàn tất pipeline, full training, đánh giá và export/reload model.
- **Trạng thái chất lượng:** Trained candidate; chưa đủ cơ sở khuyến nghị correction tự động hoặc triển khai lâm sàng.

## 1. Kết luận tổng quát

**Quy trình huấn luyện đã được triển khai và chạy thành công, nhưng model mới chưa đủ tốt để khuyến nghị bật correction tự động.**

Model giảm được một ít lỗi token trên official test so với transcript ASR raw. Tuy nhiên, nó đồng thời:

- Làm kết quả trên dev xấu hơn.
- Tăng lỗi ký tự trên test.
- Tăng lỗi bỏ sót nội dung theo alignment dùng để chấm.
- Thay đổi cả những transcript tham chiếu được coi là đúng trong phép thử preservation.
- Chưa cho thấy lợi ích rõ ràng hơn model correction có sẵn, chưa fine-tune thêm.

Cần phân biệt hai kết luận:

| Câu hỏi | Kết quả |
|---|---|
| Có xây được pipeline chạy hoàn chỉnh không? | **Có.** Đọc dữ liệu, chạy ASR, tạo training pairs, train LoRA, đánh giá, export và reload đều đã chạy thực tế. |
| Có train ra một corrector đủ tốt để thay thế cách làm hiện tại không? | **Chưa đạt.** Lợi ích chất lượng còn nhỏ và đi kèm các dấu hiệu gây hại. |

Vì vậy, **“đã hoàn tất” là hoàn tất công việc triển khai và thí nghiệm**, không có nghĩa đã có model tốt để đưa vào sử dụng. Đây là một thí nghiệm có kết quả chưa đạt kỳ vọng, không phải notebook mẫu hoặc một lượt training chưa chạy xong.

## 2. Phạm vi và đối tượng được huấn luyện

### 2.1. Pipeline

```text
Audio
  → Frozen ASR tạo transcript
  → Corrector nhận transcript và sửa văn bản
  → Transcript sau correction
```

Trong thí nghiệm này:

- **ASR được giữ nguyên**, không huấn luyện lại.
- Phần được fine-tune là **corrector sửa văn bản sau ASR**.
- Corrector **không nghe audio**, chỉ nhìn transcript.
- Dữ liệu supervision chính là `ASR hypothesis → reference` của cùng audio train.
- Không đưa dữ liệu QA question→answer hoặc các nguồn phụ chưa được phê duyệt vào training.

Việc corrector không nghe audio là một giới hạn quan trọng. Nếu ASR nghe sai tên thuốc nhưng tạo ra một cụm từ vẫn có vẻ hợp lý, corrector không có âm thanh để đối chiếu. Nó có thể sửa đúng, nhưng cũng có thể đoán sai hoặc bỏ bớt nội dung.

### 2.2. Ý nghĩa các run

| Run | Ý nghĩa |
|---|---|
| **R0** | Giữ nguyên transcript ASR, không correction. |
| **R1** | Sửa bằng rule học từ train. Trong lượt này không có rule đạt ngưỡng đã đặt ra, nên output giống R0. |
| **R2** | Dùng model correction có sẵn, chưa fine-tune thêm trong thí nghiệm này. |
| **R3** | Model correction được fine-tune bằng dữ liệu lỗi ASR của VietMed. |

Model correction nền là `bmd1905/vietnamese-correction-v2`. ASR chính là checkpoint `asr/whisper-small-vietnamese/checkpoint-5000` thuộc `leduckhai/MultiMed-ST`.

Ba seed **42, 43, 44** là ba lượt R3 độc lập, cùng công thức huấn luyện nhưng khác yếu tố ngẫu nhiên. Mục đích là tránh kết luận chỉ từ một lượt may mắn.

### 2.3. Những gì đã chạy thực tế

- Notebook đã chạy **13/13 code cells từ kernel mới** bằng notebook execution runner.
- ASR cache có đủ **2.773 train, 2.912 dev, 3.437 test và 85 CV records**.
- Dev được tách thành **2.769 dev_tune** và **143 dev_shared_recording**; phần shared recording không dùng để chọn model.
- Training pairs gồm **5.544 pairs**, bao gồm real pairs và identity views.
- R3 seeds **42, 43, 44** đều hoàn tất **3 epoch và 1.041 optimizer updates**.
- Cả ba chọn checkpoint **epoch 1** bằng generated dev WER theo policy đã khóa.
- Đã đánh giá sáu run R0/R1/R2/R3_seed42/R3_seed43/R3_seed44 trên dev_tune, official test và hai tập diagnostic.

Dừng ở epoch 3 là đúng quy tắc early stopping. **“Tối đa 5 epoch” không có nghĩa bắt buộc phải chạy hết 5 epoch.** Các lượt này hoàn tất theo protocol, không phải bị ngắt giữa chừng.

## 3. Bảng kết quả chính

Corpus metrics được tính trên toàn bộ token/ký tự của tập đánh giá, không thay bằng trung bình WER từng câu.

| Run | WER dev_tune (%) | WER official test (%) | CER official test (%) |
|---|---:|---:|---:|
| R0 — không correction | 4,436 | 28,349 | 21,364 |
| R1 — train-only rules | 4,436 | 28,349 | 21,364 |
| R2 — correction zero-shot | 5,248 | 27,895 | 21,505 |
| R3 seed 42 — adapter | 5,362 | 27,983 | 21,601 |
| R3 seed 43 — adapter | 5,418 | 27,861 | 21,519 |
| R3 seed 44 — adapter | 5,734 | 27,898 | 21,642 |
| **Final model merged — chấm riêng** | — | **27,924** | **21,556** |

- Dev_tune: **2.769 mẫu**.
- Official test: **3.437 mẫu**.
- Dấu `—` nghĩa là bảng không báo một lượt chấm dev riêng cho file merged; không dùng kết quả test của file này để chọn lại model.
- R1 trùng R0 vì không có rule đạt ngưỡng train-only đã khóa. Không nới ngưỡng sau khi xem test.

### 3.1. Biến động giữa các seed

R3 adapter có **test WER mean ± sample standard deviation là 27,914% ± 0,063%**.

Delta WER trung bình so với R0 là **−0,434 điểm phần trăm**. CI 95% từ **2.000 paired recording-cluster bootstrap replicates** là **[−0,540; −0,350] điểm phần trăm**.

Khoảng này cho thấy tín hiệu giảm WER so với R0 trong phép đánh giá đã thực hiện. Tuy nhiên:

- Đây không phải bằng chứng R3 vượt R2.
- Đây không phải bằng chứng về độ an toàn y khoa.
- Đây không phải bằng chứng giảm công chỉnh sửa của người dùng.
- Số recording ít; bootstrap theo recording không bao phủ mọi nguồn bất định.
- Test đã được tiếp xúc trong các thí nghiệm lịch sử; không mô tả nó như một tập hoàn toàn chưa từng được xem.

## 4. WER giảm từ 28,349% xuống 27,924% có nghĩa là gì?

### 4.1. Cách hiểu WER

```text
WER = (số lỗi thay thế + số lỗi bỏ sót + số lỗi thêm thừa)
      / số token trong transcript tham chiếu
```

**WER càng thấp càng tốt.** Nhưng WER không phải tỷ lệ câu sai và không trực tiếp là độ chính xác y khoa.

Trong cách chấm hiện tại, token được phân tách theo khoảng trắng. Với tiếng Việt, không nên hiểu chúng hoàn toàn tương đương “từ” theo nghĩa ngôn ngữ học.

### 4.2. Quy đổi sang số lỗi thực tế

Trên official test có **76.138 token tham chiếu**:

| Chỉ số | Không correction — R0 | File final model |
|---|---:|---:|
| Tổng lỗi token | 21.584 | 21.261 |
| WER | 28,349% | 27,924% |

Model **giảm ròng 323 lỗi token** trên toàn bộ test.

“Giảm ròng” không có nghĩa model chỉ sửa đúng 323 lỗi và không gây lỗi mới. Nó vừa sửa được lỗi cũ, vừa tạo thêm lỗi ở những chỗ khác.

### 4.3. Mức giảm tuyệt đối và tương đối

- **Giảm tuyệt đối:** khoảng **0,424 điểm phần trăm WER**.
- **Giảm tương đối:** khoảng **1,50% số lỗi so với R0**.

Không phải giảm 28% lỗi, cũng không phải giảm 1,5 điểm phần trăm.

Theo ngưỡng đã đặt ra, cần giảm ít nhất **2% tương đối**. Với R0 hiện tại, ngưỡng đó tương ứng WER khoảng **27,782% hoặc thấp hơn**. File final đạt **27,924%**, nên chưa đạt điều kiện này.

**Không đạt ngưỡng 2% không phải lý do duy nhất để chưa khuyến nghị model.** Những vấn đề về bỏ sót, preservation và kết quả dev cũng cần được xem xét.

## 5. Model đang sửa được gì và đang làm hỏng gì?

### 5.1. Phân rã lỗi trên official test

| Loại lỗi | R0 | Final model | Thay đổi |
|---|---:|---:|---:|
| Thay thế sai | 10.409 | 9.786 | Giảm 623 |
| Bỏ sót | 5.585 | 6.046 | **Tăng 461** |
| Thêm thừa | 5.590 | 5.429 | Giảm 161 |
| **Tổng** | **21.584** | **21.261** | **Giảm 323** |

Theo alignment dùng để chấm, model giảm lỗi thay thế và thêm thừa nhưng lại tăng lỗi bỏ sót.

Vì vậy, không thể chỉ nhìn WER tổng rồi kết luận correction tốt hơn ở mọi khía cạnh.

### 5.2. Thay đổi ở mức từng mẫu

Trên 3.437 mẫu test:

- **573 mẫu** có ít lỗi token hơn.
- **310 mẫu** có nhiều lỗi token hơn.
- **2.554 mẫu** giữ nguyên số lỗi.

“Giữ nguyên số lỗi” không nhất thiết là giữ nguyên văn bản. Model có thể thay đổi câu nhưng tổng số lỗi vẫn bằng nhau.

### 5.3. Vì sao lỗi bỏ sót đáng chú ý trong miền y tế?

WER tính mỗi lỗi token theo cùng một đơn vị, nhưng mức độ quan trọng của nội dung không giống nhau.

**Ví dụ minh họa, không phải lỗi y khoa đã được xác nhận của model:**

```text
“không dị ứng penicillin”
→ “dị ứng penicillin”
```

Chỉ bỏ một token nhưng ý nghĩa có thể đảo ngược.

Do đó, giảm tổng lỗi chưa đủ để chứng minh model giữ được tên thuốc, liều lượng, đơn vị và phủ định. Những phần này cần review riêng, đối chiếu với audio khi đánh giá nội dung thực tế.

## 6. Overcorrection 17,95% nghĩa là gì?

### 6.1. Phép kiểm tra preservation

```text
Transcript tham chiếu
  → Đưa vào corrector
  → Kiểm tra corrector có giữ nguyên hay không
```

Transcript tham chiếu được coi là đầu vào đúng **theo chuẩn chấm của dataset**. Một corrector biết khi nào không nên sửa cần giữ được những đầu vào này.

Đối với file final model:

- Đưa vào **3.437 transcript tham chiếu**.
- **617 transcript** bị thay đổi sau scoring normalization.
- Tỷ lệ reference overcorrection là **17,95%**.

Nói dễ hiểu: cứ khoảng 100 transcript tham chiếu đưa vào, model thay đổi khoảng 18 transcript, dù mục tiêu của phép thử là giữ nguyên chúng.

### 6.2. Những điều không nên suy diễn

1. **Không phải 17,95% tổng số token bị sai.** Đây là tỷ lệ mẫu có thay đổi.
2. Các thay đổi được đếm không chỉ là khác hoa/thường hoặc dấu câu bị scoring normalization bỏ qua.
3. Chưa thể gọi cả 617 trường hợp là lỗi y khoa nghiêm trọng. Muốn biết mức độ tác hại cần review chuyên môn.
4. Transcript tham chiếu là chuẩn chấm của dataset, không thay thế việc thẩm định tính đúng đắn y khoa của từng câu.

Dù có các giới hạn trên, phép thử đã cho thấy **model chưa bảo toàn đầu vào đúng đủ tốt theo transcript tham chiếu**.

## 7. Vì sao dev xấu hơn nhưng test lại tốt hơn một chút?

### 7.1. Hai tập có mức lỗi ASR khác nhau rõ rệt

| Chỉ số | Dev_tune | Official test |
|---|---:|---:|
| R0 — ASR chưa correction | 4,436% WER | 28,349% WER |

Trong lượt chạy này, ASR trên dev chính xác hơn nhiều so với trên test. Không nên mặc định hai tập có cùng độ khó đối với ASR.

### 7.2. Kết quả dev của R3 seed 42

```text
4,436% → 5,362% WER
```

Đây là **tăng khoảng 20,9% số lỗi tương đối**.

Số liệu cụ thể:

- Tổng lỗi tăng từ **2.909 lên 3.516**: thêm ròng **607 lỗi**.
- **71 mẫu** tốt lên.
- **552 mẫu** xấu đi.
- **2.146 mẫu** giữ nguyên số lỗi.

Đây là tín hiệu bất lợi rõ ràng đối với việc dùng model làm bước correction mặc định.

### 7.3. Diễn giải và giới hạn của diễn giải

**Giả thuyết cần kiểm chứng [INFERENCE]:** khi đầu vào còn nhiều lỗi, corrector có chỗ để sửa và tạo ra lợi ích ròng nhỏ; khi đầu vào vốn khá đúng, việc can thiệp lại dễ tạo thêm lỗi. Phép thử overcorrection cũng cho thấy model có vấn đề với việc giữ nguyên văn bản đúng.

Tuy nhiên, chưa đủ bằng chứng để chốt nguyên nhân gốc là overfitting, sampler, dữ liệu hay thiết kế loss. Muốn kết luận nguyên nhân cần phân tích thêm trên train/dev, không chỉ nhìn bảng WER.

## 8. WER giảm nhưng CER tăng có mâu thuẫn không?

Không. Hai metric đếm lỗi ở hai đơn vị khác nhau:

- **WER:** mức token.
- **CER:** mức ký tự.

Một token dài và một token ngắn đều có thể tính là một lỗi token, nhưng số ký tự sai rất khác nhau.

Kết quả file final:

```text
WER: 28,349% → 27,924%    tốt hơn
CER: 21,364% → 21,556%    xấu hơn
```

Model giảm được tổng lỗi token nhưng làm tăng tổng lỗi ký tự. Đây không phải một kết quả cải thiện đồng đều.

**Không nên chọn metric đang đẹp rồi bỏ qua metric đang xấu.** Cả hai cần được báo, cùng với các lỗi nội dung quan trọng và preservation.

## 9. Fine-tuning có tốt hơn dùng model có sẵn không?

**Chưa có bằng chứng thuyết phục trong thí nghiệm này.**

| Phương án | WER official test |
|---|---:|
| Model có sẵn — R2 | **27,895%** |
| R3 trung bình ba seed | 27,914% |
| File final merged | 27,924% |

Model có sẵn đã đạt WER test thấp hơn một chút so với trung bình R3 và file final.

Seed 43 có WER test **27,861%**, tốt hơn R2 một chút. Tuy nhiên:

- Chênh lệch nhỏ.
- Không thể chỉ chọn seed có test tốt nhất rồi coi đó là kết quả đại diện.
- CI đã báo so với R0 không phải một phép chứng minh R3 vượt R2.
- Chưa có kết luận từ các số này rằng R3 vượt R2 một cách chắc chắn.

Phát biểu phù hợp là:

> Fine-tuning theo công thức hiện tại chưa chứng minh được giá trị tăng thêm rõ ràng so với corrector có sẵn.

Điều này không có nghĩa LoRA hoặc correction tiếng Việt không hiệu quả nói chung. Nó chỉ nói về công thức và dữ liệu đã thử ở lượt này.

### 9.1. Vì sao vẫn xuất seed 42?

Quy tắc đã khóa là chọn bằng **dev**, không chọn bằng test.

Trong ba model R3:

- Seed 42 có dev WER tốt nhất.
- Seed 42 được chọn để export trước khi chấm test.
- Không đổi sang seed 43 sau khi thấy test của seed 43 tốt hơn.

Đây là **model tốt nhất trong ba ứng viên R3 theo tiêu chí chọn**, không phải model tốt nhất trong mọi phương án. Trên dev, R0 vẫn tốt hơn cả ba.

## 10. Export/reload thành công chứng minh điều gì?

### 10.1. Ý nghĩa kỹ thuật

Export/reload thành công chứng minh rằng:

- Trọng số đã được lưu và tải được bằng Transformers.
- Có thể chạy trong process mới.
- Không cần object model cũ trong RAM.
- Không cần Hugging Face cache chứa base model cũ.
- Chỉ đưa ASR text vào là chạy được inference.

Các kiểm tra đã thực hiện:

- Merged model trước/sau reload khớp **8/8 probe outputs**.
- Subprocess mới chạy ngoài repo, với **Hugging Face cache rỗng**, tải local-only và tái tạo đúng **8/8 outputs**.
- Inference probe chỉ nhận ASR text, không nhận reference.
- Final model sử dụng `MBartForConditionalGeneration` và `BartphoTokenizer`.

Đây là kiểm tra **khả năng bàn giao và tái hiện**, không phải bằng chứng về chất lượng y khoa.

### 10.2. Vì sao adapter và file merged có metric khác nhau?

Adapter và merged model biểu diễn phép tính theo cách khác nhau. Khi dùng BF16, việc làm tròn có thể làm thay đổi lựa chọn token trong quá trình generation.

Đã điều tra bằng FP32 controls:

- Generated outputs ở FP32 khớp trên các probe đã cố định.
- Sai khác logits nằm trong tolerance đã kiểm tra.
- Không thay checkpoint, không đổi precision triển khai và không chọn lại model bằng test.

Trên official test, file merged khác adapter ở **183 raw outputs**, trong đó **171/3.437 outputs vẫn khác sau scoring normalization**. Vì vậy, chính file merged đã được chấm riêng.

Cần đọc đúng hai kết quả:

- **27,983% WER:** adapter seed 42.
- **27,924% WER:** file final merged đã xuất.

Không lấy kết quả tốt hơn của hai dạng rồi gán cho cả hai.

## 11. Kết luận cho đồ án và quyết định sử dụng

### 11.1. Kết luận nghiên cứu

> Đã triển khai thành công pipeline fine-tune correction hậu ASR trên VietMed bằng LoRA. Các lượt R3 hoàn tất và cho thấy mức giảm WER nhỏ trên official test so với ASR raw. Tuy nhiên, kết quả dev suy giảm, CER test tăng, khả năng bảo toàn transcript đúng còn hạn chế và chưa cho thấy lợi ích rõ ràng so với baseline zero-shot. Vì vậy, chưa đủ cơ sở khuyến nghị tích hợp correction tự động theo cấu hình hiện tại.

Giá trị của thí nghiệm nằm ở việc đã đo được cả mặt lợi và mặt hại, thay vì chỉ báo training loss giảm.

### 11.2. Quyết định sử dụng hiện tại

- **Giữ R0 làm mặc định theo protocol hiện tại.** Điều này không có nghĩa transcript ASR raw đã an toàn hoặc không cần kiểm tra.
- Giữ model đã train làm ứng viên nghiên cứu, chưa tự động ghi đè transcript ASR.
- Chưa tuyên bố giảm công chỉnh sửa vì chưa có user study đo việc đó.
- Chưa tuyên bố an toàn y khoa vì chưa có review chuyên môn tương ứng.
- NER được báo là **consistency**, không phải gold NER F1 và không thay thế review nội dung.

### 11.3. Các nhánh chưa thực hiện

**R4 và R3-budget vẫn tắt** do thiếu manifest phê duyệt đầy đủ về:

- Quyền sử dụng nguồn phụ.
- Clean targets đã được review.
- Parent/source grouping, dedup và group split.
- Synthetic noise provenance từ ASR train error profile.
- Thiết kế đối chứng matched-budget tương ứng khi bật R4.

Không tự coi các dataset đã tải về là dữ liệu đủ điều kiện để trộn vào training. Việc các nhánh này bị tắt không làm phần R3 trở thành một lượt chưa hoàn tất.

## 12. Hướng tiếp theo hợp lý

Ưu tiên phân tích lỗi trên train/dev và review set đã xuất:

1. **Phân tích lỗi bỏ sót:** nội dung nào bị mất, độ dài câu nào dễ gặp, recording hoặc dạng lỗi ASR nào liên quan.
2. **Phân tích overcorrection:** những transcript tham chiếu bị sửa thuộc nhóm nào; phân biệt thay đổi vô hại, khác biệt reference và sai lệch nội dung.
3. **Review các thành phần quan trọng:** tên thuốc, số/liều, đơn vị, phủ định và nội dung thêm không có trong audio.
4. **Sau đó mới quyết định thay đổi dữ liệu hoặc cách huấn luyện**, dựa trên bằng chứng từ train/dev và review.

Không nên tiếp tục đổi seed hoặc công thức dựa trên test cho đến khi tìm được một bảng đẹp. Nếu kết quả hiện tại dẫn đến thiết kế nghiên cứu mới, cần khai báo chu kỳ mới và không mô tả cùng test như một tập chưa từng được tiếp xúc.

**Tóm lại: phần kỹ thuật đã thành công; model mới có một chút lợi ích về WER test, nhưng lợi ích đó chưa bù được các dấu hiệu gây hại và chưa chứng minh hơn baseline có sẵn. Vì vậy, “đã train xong” nhưng “chưa nên bật correction tự động”.**

## 13. Artifact và bằng chứng đối chiếu

### 13.1. Notebook, model và dữ liệu derived

- [Notebook end-to-end](experiments/002-vietmed-correction-training/train_correction_end_to_end.ipynb)
- [Final model directory](experiments/002-vietmed-correction-training/models/final_model/)
- Adapters đã chọn:
  - `experiments/002-vietmed-correction-training/models/full/R3_seed42/epoch-1/`
  - `experiments/002-vietmed-correction-training/models/full/R3_seed43/epoch-1/`
  - `experiments/002-vietmed-correction-training/models/full/R3_seed44/epoch-1/`
- ASR cache, split/source manifests, audit và training pairs: `datasets/derived/correction/full/`.
- Báo cáo tổng hợp, predictions, bootstrap, NER consistency và review exports: `experiments/002-vietmed-correction-training/outputs/full/`.

### 13.2. Các receipt và báo cáo chính

- [Final summary](experiments/002-vietmed-correction-training/outputs/full/final-summary.json)
- [Notebook execution receipt](experiments/002-vietmed-correction-training/outputs/full/notebook-execution.json)
- [Training run receipts](experiments/002-vietmed-correction-training/outputs/full/training-runs.json)
- [Delivery audit](experiments/002-vietmed-correction-training/outputs/full/delivery-audit.json)
- [Export verification](experiments/002-vietmed-correction-training/outputs/full/export-verification.json)
- [Offline inference verification](experiments/002-vietmed-correction-training/outputs/full/offline-inference-verification.json)
- [Standalone exported-model test comparison](experiments/002-vietmed-correction-training/outputs/full/exported-model-test.json)
- [R0 official test summary](experiments/002-vietmed-correction-training/outputs/full/evaluation/test_official/R0/summary.json)
- [Final model official test summary](experiments/002-vietmed-correction-training/outputs/full/evaluation/test_official/FinalModel_seed42/summary.json)
- [R3 seed 42 dev summary](experiments/002-vietmed-correction-training/outputs/full/evaluation/dev_tune/R3_seed42/summary.json)
- [Official test bootstrap comparison](experiments/002-vietmed-correction-training/outputs/full/bootstrap/test_official/comparison.json)

Audit đã tính lại metric cho **24 tổ hợp run/split**, kiểm tra hashes và xác nhận **133 file lịch sử nguyên vẹn** so với baseline đã ghi nhận. File final model cũng được kiểm tra và chấm riêng.

Các đường dẫn artifact trong báo cáo trỏ đến dữ liệu local. Models, datasets và outputs đang được Git ignore; một bản clone chỉ chứa code có thể không có các artifact này. Báo cáo không đồng nghĩa đã công bố weights, dataset hoặc review text lên remote.
